version: "1.0"
rules:
  - action: "post"
    resource: "/twitter"
    conditions:
      maxContentLength: 280
      allowedPlatforms: ["twitter"]
  - action: "read"
    resource: "/user/profile"
  - action: "post"
    resource: "/email"
    conditions:
      maxContentLength: 10000
